# git-push

Simple shell script I use to automate git add, commit, and push.

## Install

```sh
curl -o gitpush.sh https://raw.githubusercontent.com/stefanrogic/git-push/main/gitpush.sh && chmod +x gitpush.sh
```

## Usage

```sh
./gitpush.sh "your commit message"
```
